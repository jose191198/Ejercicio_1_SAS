DATA VENTAS;
INPUT NAMES$ WEEK1-WEEK4;
TOTAL = WEEK1 + WEEK2 + WEEK3 + WEEK4;
NOMBRE2 = NAMES;
DATALINES;
JUAN 10 2 40 0
PEDRO 15 5 10 20
LUISA 50 10 0 30
MARIO 20 30 45 0
;
RUN;

PROC SQL;
SELECT * FROM VENTAS WHERE TOTAL > 70;
QUIT;

PROC PRINT DATA = VENTAS (WHERE = (TOTAL > 70));RUN;

PROC PRINT DATA = VENTAS; WHERE TOTAL > 70; RUN;

/* NO ES RECOMENDABLE USAR ESTA FORMA */
DATA VENTAS2;
SET VENTAS;
WHERE TOTAL > 70;
RUN;